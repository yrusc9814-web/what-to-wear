# 小衣橱 V1 正式 PRD

版本：V1.4  
状态：正式开发基线·收口版  
产品形态：微信小程序  
项目仓库：`https://github.com/yrusc9814-web/what-to-wear.git`

---

# 0. 文档定位

本 PRD 是「小衣橱」V1 的正式产品基线，用于统一产品目标、页面结构、核心流程、业务规则和 V1 完成定义。

详细页面布局、操作细节、验收项分别以以下开发文档为准：

```text
第一栏/小衣橱-首页开发文档-V1.md
第二栏/小衣橱-衣橱开发文档-V1.md
第三栏/小衣橱-穿搭开发文档-V1.md
小衣橱-三子代理公共接口与开发边界说明-V1.md
小衣橱-子页面统一视觉规范-V1.md
AGENTS.md
```

发生冲突时，优先级为：

```text
用户最新明确确认
→ 对应栏目最新开发文档
→ 三子代理公共接口与开发边界说明
→ 本 PRD
→ 现有代码
```

V1.4 已覆盖 V1.3 中仍处于待确认状态的以下事项：

- 正式页面名称统一为「穿搭」。
- 衣橱手机端默认两列网格。
- 保留两列网格 / 列表视图切换。
- 衣橱筛选确定为季节 / 风格 / 主颜色 / 厚度，V1 均为单选。
- 单品主颜色确定为单选。
- 删除衣橱单品后，历史穿搭保留快照。
- 首页快速开始搭配改为先展示历史搭配组合。
- 「换一套」改为按当前季节、风格不限，从有效衣橱中随机推荐。
- 保存搭配使用名称 / 季节 / 风格确认流程。
- 编辑已有搭配支持「更新原搭配 / 另存为新搭配」。
- 匹配结果不得直接设置今日穿搭或直接进入编辑。

---

# 1. 产品定位

「小衣橱」是一个面向女生的个人衣橱与穿搭保存微信小程序。

核心价值不是自动替用户决定穿什么，而是：

```text
把自己的衣服录入衣橱
→ 方便分类和查找
→ 自己组合穿搭
→ 保存喜欢的组合
→ 随时复用历史搭配
→ 根据天气、季节和风格快速找到适合今天的穿搭
```

AI 在 V1 中只承担辅助识别和描述生成，不替代用户完成最终选择。

---

# 2. V1 核心闭环

```text
上传单品
→ AI 辅助识别
→ 用户确认并保存
→ 衣橱分类管理
→ 进入穿搭
→ 手动选择单品 / 当前季节随机换一套
→ 查看叠穿平铺预览
→ 保存搭配
→ 首页展示最近保存
→ 首页按季节 + 风格筛选已保存搭配
→ 用户进入详情确认
→ 编辑或设置为今日穿搭
```

---

# 3. V1 页面结构

底部固定三个主栏目：

| 栏目 | 正式名称 | 核心作用 |
|---|---|---|
| 栏目 1 | 首页 | 今日穿搭入口、天气建议、最近保存、智能筛选 |
| 栏目 2 | 衣橱 | 上传、分类、搜索、筛选、查看和管理单品 |
| 栏目 3 | 穿搭 | 五类选衣、叠穿预览、换一套、保存和管理搭配 |

V1 不新增第四个底部栏目。

主要子页面：

| 子页面 | 所属业务 | 进入方式 |
|---|---|---|
| 个人中心 | 全局 | 点击头像 |
| 城市选择 | 首页 | 点击当前城市 |
| 匹配穿搭 | 首页 | 点击「查看匹配穿搭」 |
| 上传新单品 | 衣橱 | 点击「上传新单品」 |
| 单品详情 / 编辑 | 衣橱 | 点击单品 |
| 历史搭配选择 | 穿搭 | 首页「快速开始搭配」进入 |
| 我的搭配 | 穿搭 | 点击「我的搭配」 |
| 穿搭详情 | 首页 / 穿搭 | 点击已保存搭配 |

---

# 4. 全局视觉原则

视觉以已确认参考图为最高依据。

整体风格：

- 女性化、轻盈、柔和。
- 粉色 / 浅粉为主强调色。
- 暖白或极浅粉背景。
- 大圆角卡片。
- 浅色边框。
- 柔和阴影。
- 页面保持充足留白。
- 文字使用深灰 / 暖灰层级，不大面积使用纯黑。
- 禁止斜体。
- 衣物图片以完整显示主体为优先，不得随意裁切。
- 页面主要布局、模块顺序和卡片比例不得以“优化”为名擅自重排。

---

# 5. 栏目 1：首页

## 5.1 页面目标

首页负责：

1. 快速开始今天的穿搭。
2. 展示当前位置天气和穿搭建议。
3. 快速回看最近保存的搭配。
4. 从已保存搭配中按季节和风格筛选适合当前需求的组合。

首页不临时从衣橱生成新穿搭。

## 5.2 页面结构

从上到下固定为：

```text
顶部标题区
→ 今日搭配启动卡
→ 天气建议
→ 最近保存穿搭
→ 智能穿搭建议
→ 底部导航
```

---

## 5.3 顶部标题区

展示：

```text
小衣橱
今日穿搭灵感
```

右侧为用户头像。

点击头像：

```text
→ 个人中心
```

---

## 5.4 今日搭配启动卡

保持参考图的大卡结构，展示：

- 日期。
- 「开始今天的搭配」。
- 辅助说明。
- 「快速开始搭配」。
- 穿搭预览。
- 当前城市。
- 当前温度。
- 天气状态。

参考图中无业务意义的轮播圆点正式删除。

### 快速开始搭配

点击后：

```text
首页
→ 历史搭配选择
→ 只展示用户以前保存过的搭配组合
→ 不自动选中任何一套
```

用户点击某套历史组合：

```text
→ 穿搭详情
```

如果都不满意：

```text
点击「去搭配单品」
→ 进入穿搭主页面
→ 自己重新选择单品
```

按钮文案固定：

```text
去搭配单品
```

没有历史搭配时，直接显示空状态和「去搭配单品」。

---

## 5.5 天气建议

展示：

- 天气图标。
- 天气状态。
- 温度区间。
- 一句基础穿搭建议。

城市支持：

```text
重新定位
+
手动搜索 / 选择城市
```

城市修改后必须同步更新：

- 当前城市。
- 当前温度。
- 天气状态。
- 天气图标。
- 温度区间。
- 穿搭建议。

定位失败时允许手动选择城市。

天气请求失败不得影响首页其他模块。

---

## 5.6 最近保存穿搭

首页展示最近最多 4 套已保存穿搭。

排序：

```text
最新保存优先
```

每张卡展示：

- 叠穿平铺缩略图。
- 名称。
- 保存日期。

点击单张：

```text
→ 穿搭详情
```

不得直接进入编辑。

点击：

```text
全部
```

进入：

```text
我的搭配
```

---

## 5.7 智能穿搭建议

产品定义：

```text
根据用户选择的季节和风格
从用户已经保存过的穿搭中筛选
```

不是生成新搭配。

### 季节

单选：

```text
春
夏
秋
冬
```

### 风格

单选：

```text
不限
休闲
通勤
甜美
潮酷
```

「不限」只代表不限风格，不代表不限季节。

### 查看匹配穿搭

```text
选择季节
→ 选择风格
→ 查看匹配穿搭
→ 进入独立匹配结果页
→ 展示所有符合条件的已保存穿搭
```

不得只推荐一套。

---

## 5.7.1 默认筛选状态

首页默认：

```text
季节：当前季节
风格：不限
```

当前季节统一按设备本地月份：3～5 月春、6～8 月夏、9～11 月秋、12～2 月冬。

## 5.8 匹配结果规则

匹配结果列表中：

- 只展示已保存穿搭。
- 点击某套后进入穿搭详情。
- 不允许在列表卡片上直接设置今日穿搭。
- 不允许在列表卡片上直接进入编辑。

正确流程：

```text
匹配结果
→ 穿搭详情
→ 用户进一步选择：
   编辑
   或
   设为今日穿搭
```

---

## 5.9 今日穿搭

只有用户在穿搭详情中主动确认后才能设置：

```text
穿搭详情
→ 设为今日穿搭
→ 用户确认
→ 保存
```

同一时间最多一套有效今日穿搭。

统一事实源：

```ts
todayOutfit: { date: string, outfitId: string } | null
```

其中 `date` 为设备本地日期 `YYYY-MM-DD`。

跨日规则已确认：

```text
进入下一天
→ 昨日今日穿搭失效
→ 当前显示“没有今日穿搭”
```

不得在 `SavedOutfit` 内同时维护 `isToday` 形成第二事实源。

任何推荐、匹配、历史卡片或「换一套」都不得自动设置。
---

# 6. 栏目 2：衣橱

## 6.1 页面目标

衣橱负责：

- 管理用户自己的单品。
- 快速查找单品。
- AI 辅助录入。
- 编辑和删除。
- 为穿搭栏目提供有效单品数据。

---

## 6.2 正式分类

V1 只保留五类：

```text
帽子
上衣
下装
鞋子
包包
```

对应值：

```text
hat
top
bottom
shoes
bag
```

「配饰」属于旧设计残留，不作为正式分类。

---

## 6.3 主页面结构

```text
顶部标题
→ 搜索 / 视图 / 筛选
→ 分类标签
→ 已收纳数量 + 上传新单品
→ 单品内容区
→ 底部导航
```

---

## 6.4 视图

手机端默认：

```text
两列网格
```

保留「视图」切换：

```text
两列网格
↔
列表视图
```

用户切换后记住上一次视图。

不得恢复默认三列网格。

---

## 6.5 搜索

搜索范围包括：

- 单品名称。
- 分类。
- 主颜色。
- 风格。
- 备注。

搜索可与分类和筛选同时生效。

---

## 6.6 分类标签

固定：

```text
全部
帽子
上衣
下装
鞋子
包包
```

单选。

移动端宽度不足时允许横向滚动。

---

## 6.7 筛选

V1 只做：

```text
季节
风格
主颜色
厚度
```

四类筛选目前全部单选。

主颜色正式枚举：粉色、白色、黑色、米色、灰色、蓝色、棕色、绿色、红色、黄色、紫色、多色、其他。

筛选面板包含：

```text
重置
查看结果
```

有筛选生效时，顶部筛选入口应显示明显状态。

注意：

- 单品本身的适用季节可以多选。
- 单品本身的风格可以多选。
- 单品主颜色单选。
- 单品厚度单选。
- 衣橱筛选条件则全部为单选。

---

## 6.8 单品展示

网格模式每张卡主要展示：

- 图片。
- 名称。
- 分类。
- 更多操作。

列表模式主要展示：

- 缩略图。
- 名称。
- 分类。
- 主颜色。
- 季节 / 风格简要标签。
- 更多操作。

点击单品：

```text
→ 单品详情
```

更多操作：

```text
查看
编辑
修改分类
删除
```

删除必须二次确认。

---

# 7. 上传新单品

## 7.1 三步流程

固定为：

```text
1 上传图片
→
2 AI 识别信息
→
3 确认保存
```

---

## 7.2 第一步：上传图片

支持：

- 拍照。
- 从相册选择。

V1 每个单品保存 1 张主图。

建议图片：

- 主体完整。
- 光线清晰。
- 背景尽量简单。
- 衣物位于画面主体区域。

上传失败支持重试。

---

## 7.3 AI 可识别内容

AI 可以提供建议值：

- 单品名称。
- 分类。
- 主颜色。
- 适用季节。
- 风格。
- 厚度。
- AI 单品介绍。

所有 AI 结果都必须允许用户修改。

分类最终由用户确认。

---

## 7.4 AI 不得自动编造

仅凭图片不得自动填写：

- 购买价格。
- 购买日期。
- 购买渠道。
- 尺码。
- 品牌。
- 真实材质成分。
- 其他无法可靠确认的信息。

AI 识别失败时，用户仍可手动填写并继续保存。

---

## 7.5 必填信息

| 字段 | 规则 |
|---|---|
| 单品图片 | 必填 |
| 单品名称 | 必填 |
| 分类 | 单选必填 |
| 适用季节 | 多选，至少 1 项 |
| 风格 | 多选，至少 1 项 |

---

## 7.6 选填信息

| 字段 | 规则 |
|---|---|
| 主颜色 | 单选 |
| 厚度 | 薄 / 适中 / 厚 |
| 尺码 | 文本 |
| 购买价格 | 数字 |
| 购买日期 | 日期 |
| 购买渠道 | 文本 |
| AI 单品介绍 | 可编辑、可重新生成 |
| 备注 | 文本 |

以下字段不纳入 V1：

- 品牌。
- 收纳位置。
- 喜好程度。

---

## 7.7 保存

确认保存后：

```text
保存单品
→ 更新衣橱数量和分类
→ 同步穿搭页面对应分类的数据源
→ 返回衣橱
→ 提示保存成功
```

所有选填项为空时仍允许保存。

---

# 8. 单品编辑与删除

## 8.1 编辑

用户可修改：

- 名称。
- 分类。
- 主颜色。
- 季节。
- 风格。
- 厚度。
- 尺码。
- 购买信息。
- AI 介绍。
- 备注。

修改分类后，穿搭页对应分类同步更新。

---

## 8.2 删除规则

正式采用：

```text
删除衣橱单品
但历史穿搭保留保存时快照
```

删除后：

- 单品从衣橱消失。
- 不参与搜索和筛选。
- 不再参与新搭配。
- 不再参与「换一套」。
- 历史已保存穿搭继续正常显示。

如果用户编辑一套包含已删除单品的历史搭配：

```text
显示历史快照
+
提示该单品已从衣橱删除
+
要求重新选择对应槽位
```

不得因为删除衣橱单品造成历史穿搭丢图或损坏。

若单品图片仍被历史穿搭快照或今日穿搭引用，则云端原图不得随删除动作物理删除；只有无任何正式引用后才允许后续垃圾回收。

---

# 9. 栏目 3：穿搭

## 9.1 页面目标

用户可以：

- 自己选择五类单品。
- 实时查看叠穿组合。
- 根据当前季节随机换一套。
- 保存搭配。
- 查看和编辑历史搭配。
- 设置今日穿搭。

---

## 9.2 直接点击底部「穿搭」

用户直接点击底部 Tab：

```text
穿搭
```

进入参考图中的完整穿搭主页面。

不会先进入历史搭配选择页。

---

## 9.3 主页面结构

```text
顶部标题 + 我的搭配
→ 穿搭预览卡
→ 帽子选择
→ 上衣选择
→ 下装选择
→ 鞋子选择
→ 包包选择
→ 保存搭配
→ 底部导航
```

---

## 9.4 穿搭预览

顶部预览必须是叠穿平铺组合，不是商品图并排。

关系：

- 帽子在上方。
- 上衣在上半身。
- 下装与上衣自然衔接并适度重叠。
- 鞋子位于下装下方。
- 包包可叠放在侧面。

预览不包含：

- 真人。
- 人体轮廓。
- 虚拟模特。
- 生成的新衣服图片。

---

## 9.5 五类选择

固定顺序：

```text
帽子
上衣
下装
鞋子
包包
```

必选：

```text
上衣
下装
鞋子
```

可选：

```text
帽子
包包
```

帽子和包包允许选择「无」。

切换某一类单品时，只更新对应槽位，并立即刷新顶部预览。

---

# 10. 换一套

按钮：

```text
换一套
```

正式规则：

```text
根据当前季节
+
风格不限
+
从用户当前有效衣橱单品中随机组成一套
```

必须包含：

- 上衣。
- 下装。
- 鞋子。

帽子和包包可空。

不得：

- 使用已删除单品。
- 生成衣橱不存在的新单品。
- 自动保存。
- 自动设置今日穿搭。

当前季节可用单品不足时，应明确提示并引导去衣橱补充。

---

# 11. 保存搭配

## 11.1 保存按钮

位于五类单品选择区域之后，即页面内容最底部。

不使用固定悬浮保存按钮遮挡手机内容。

---

## 11.2 保存信息

点击「保存搭配」后确认：

```text
搭配名称 *
季节 *
风格 *
```

规则：

- 名称必填。
- 季节单选。
- 风格单选。
- 「不限」不能作为保存风格值。
- 上衣 / 下装 / 鞋子必须已选择。

保存成功后停留在穿搭页面。

---

# 12. 我的搭配与穿搭详情

## 12.1 我的搭配

展示全部已保存搭配，最新保存优先。

每套展示：

- 缩略图。
- 名称。
- 季节。
- 风格。
- 保存日期。

点击：

```text
→ 穿搭详情
```

---

## 12.2 穿搭详情

展示：

- 大图。
- 名称。
- 季节。
- 风格。
- 保存日期。

操作：

```text
编辑
设为今日穿搭
删除
```

点击历史搭配后必须先进入详情，不直接进入编辑。

---

# 13. 编辑已有搭配

正确流程：

```text
历史搭配 / 匹配结果
→ 穿搭详情
→ 编辑
→ 加载原五槽到穿搭主页面
→ 用户修改
```

保存修改时必须让用户选择：

```text
更新原搭配
另存为新搭配
取消
```

### 更新原搭配

- 原记录继续存在。
- 更新内容。
- 不生成新记录。

### 另存为新搭配

- 原搭配保持不变。
- 当前结果保存为新的搭配。

---

# 13.1 未保存穿搭草稿

V1 普通 Tab 切换不清空未保存穿搭草稿。

从穿搭详情点击「编辑」加载另一套搭配时，如果当前已有未保存草稿，必须先提示用户确认是否替换；未经确认不得覆盖。

---

# 14. 核心数据规则

## 14.1 季节

统一值：

```text
spring
summer
autumn
winter
```

当前季节统一按设备本地月份：

```text
3～5 月 = spring
6～8 月 = summer
9～11 月 = autumn
12～2 月 = winter
```

---

## 14.2 风格

统一值：

```text
casual
commute
sweet
cool
```

「不限」只属于筛选条件。

---

## 14.3 单品分类

统一值：

```text
hat
top
bottom
shoes
bag
```

---

## 14.4 保存搭配

每套已保存搭配至少需要：

- 唯一 ID。
- 名称。
- 季节。
- 风格。
- 五槽结构化数据。
- 保存时间。
- 更新时间。
- 可用于首页展示的预览缩略图。
- 必要的单品历史快照。

结构化五槽数据是主数据，缩略图仅用于展示。

---

## 14.5 历史快照

历史搭配中的单品必须保留必要快照。

目的：

```text
原衣橱单品以后被删除
→ 历史穿搭仍然能正常显示
```

---

## 14.6 今日穿搭

只使用一个事实源：

```ts
todayOutfit: {
  date: string
  outfitId: string
} | null
```

不得在搭配记录中再保存 `isToday` 作为第二状态源。

读取时按用户设备本地日期校验；日期不同即视为无今日穿搭。
---

# 15. 关键用户流程

## 15.1 新增单品

```text
衣橱
→ 上传新单品
→ 拍照 / 相册
→ AI 识别
→ 用户确认和修改
→ 保存
→ 衣橱出现
→ 穿搭对应分类可选
```

## 15.2 手动搭配

```text
底部穿搭
→ 选择五类单品
→ 顶部实时预览
→ 保存搭配
→ 填名称 / 季节 / 风格
→ 保存成功
```

## 15.3 快速开始搭配

```text
首页
→ 快速开始搭配
→ 历史搭配组合
→ 用户自行选择
```

都不满意：

```text
去搭配单品
→ 穿搭主页面
```

## 15.4 首页智能筛选

```text
季节
+
风格
→ 查看匹配穿搭
→ 已保存搭配结果
→ 穿搭详情
→ 编辑 / 设为今日穿搭
```

## 15.5 编辑旧搭配

```text
穿搭详情
→ 编辑
→ 修改
→ 更新原搭配 / 另存为新搭配
```

---

# 16. 空状态与异常要求

V1 必须覆盖：

- 衣橱为空。
- 某分类无单品。
- 搜索无结果。
- 筛选无结果。
- 图片上传失败。
- AI 识别失败。
- 定位失败。
- 天气获取失败。
- 没有历史搭配。
- 智能筛选无匹配结果。
- 当前季节不足以完成「换一套」。
- 保存失败。

基本原则：

- 不整页白屏。
- 不展示假用户数据。
- 失败后尽量保留用户已经填写或选择的内容。
- 提供明确重试或下一步入口。

---

# 17. V1 明确不做

- AI 真人试穿。
- 虚拟换装。
- 人体模型。
- 人体轮廓试衣。
- 姿态切换。
- 生成新的服装图片。
- 五件商品图简单并排作为穿搭效果。
- 首页临时自动生成新穿搭。
- 正式「配饰」分类。
- 第四个底部 Tab。
- 根据缩略图反向解析结构化穿搭。
- 针对具体某件衣服写硬编码定位补丁。
- AI 自动编造购买价格、日期、渠道、尺码、品牌和材质。
- 匹配结果自动设置今日穿搭。
- 换一套自动设置今日穿搭。

---

# 18. 微信小程序工程边界

V1 保留并基于现有微信小程序工程继续开发。

必须保留：

- 微信用户身份作用域与个人数据隔离；如现有工程未完成，由主代理在公共数据层先补齐。
- 三个底部栏目。
- 首页现有天气能力。
- 衣橱上传和管理能力。
- 图片云存储通路。
- 穿搭保存、查询和删除能力。
- 历史数据兼容能力。

重点调整现有推荐 / 穿搭相关页面时，应优先检查当前工程中的：

```text
miniprogram/pages/recommendation/
```

不允许：

- 回滚旧提交覆盖当前工程。
- 破坏历史数据兼容。
- 使用临时假数据代替正式状态流。
- 擅自修改现有云函数 `TYPES`。
- 因技术方便改变已确认的产品流程。

具体开发纪律以：

```text
AGENTS.md
```

为准。

---

# 19. V1 验收标准

## 首页

- 首页四个模块顺序与参考图一致。
- 头像进入个人中心。
- 快速开始先展示历史搭配，不自动选择。
- 「去搭配单品」进入穿搭主页面。
- 城市支持重新定位和手动选择。
- 城市变化后天气完整同步。
- 最近保存最多 4 套。
- 智能建议只筛选已保存搭配。
- 匹配结果必须先进入详情。

## 衣橱

- 手机默认两列网格。
- 支持两列网格 / 列表切换。
- 支持搜索。
- 正式分类只有五类。
- 筛选仅包含季节 / 风格 / 主颜色 / 厚度，均为单选。
- 上传为三步流程。
- AI 识别结果可编辑。
- 主颜色单选。
- 购买信息等无法识别字段不由 AI 编造。
- 删除单品不破坏历史穿搭。

## 穿搭

- 底部正式名称为「穿搭」。
- 直接点击底部 Tab 进入参考图中的主页面。
- 显示五类横向单品选择。
- 顶部为叠穿平铺组合。
- 「换一套」按当前季节随机推荐，风格不限。
- 上衣 / 下装 / 鞋子必选。
- 帽子 / 包包可空。
- 保存时名称 / 季节 / 风格必填。
- 保存后停留当前页。
- 历史搭配点击后先进入详情。
- 编辑后支持更新原记录或另存新记录。
- 今日穿搭只能由用户确认设置。

## 跨栏目

- 衣橱新增后穿搭可见。
- 衣橱删除后新搭配不可选，历史搭配仍正常显示。
- 穿搭保存后首页最近保存可刷新。
- 保存的季节 / 风格能被首页正确筛选。
- 设置今日穿搭后首页可读取最新状态。
- 日期进入下一天后，昨日今日穿搭自动失效并显示为空状态。
- 所有已保存穿搭使用统一 ID 打开同一穿搭详情流程。

---

# 20. V1 完成定义

以下全部满足，才视为 V1 完成：

```text
微信小程序三栏目结构完成
+
首页视觉和四模块业务完成
+
城市 / 天气联动完成
+
五类衣橱管理完成
+
两列网格 / 列表视图完成
+
三步上传和 AI 辅助识别完成
+
单品历史快照兼容完成
+
穿搭五槽选择完成
+
叠穿平铺预览完成
+
当前季节随机换一套完成
+
搭配保存和历史管理完成
+
更新原搭配 / 另存新搭配完成
+
首页季节 / 风格筛选完成
+
今日穿搭主动确认流程完成
+
三个栏目跨模块联调完成
+
全流程不依赖真人试穿或生成式服装图片
```

未满足其中任意核心项，不应宣布 V1 正式完成。

---

# 21. V1.4 配套开发资料

V1.4 正式开发时，先阅读 `00-开发资料索引.md`，并同时使用：

```text
小衣橱_V1_正式PRD_V1.4.md
第一栏/小衣橱-首页开发文档-V1.md
第二栏/小衣橱-衣橱开发文档-V1.md
第三栏/小衣橱-穿搭开发文档-V1.md
小衣橱-三子代理公共接口与开发边界说明-V1.md
小衣橱-子页面统一视觉规范-V1.md
AGENTS.md
```

PRD 负责产品总纲。

三个栏目开发文档负责具体页面与操作。

公共接口文档负责三个子代理之间的数据和开发边界。

`AGENTS.md` 负责微信小程序工程级开发纪律与安全约束。
