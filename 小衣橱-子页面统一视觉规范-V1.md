# 小衣橱｜子页面统一视觉规范 V1

版本：V1.0  
用途：约束没有独立参考图的子页面，避免三个子代理自行设计出不同风格。

---

# 1. 适用范围

适用于：

```text
个人中心
城市选择
匹配穿搭结果
历史搭配选择
我的搭配
穿搭详情
单品详情
单品编辑
衣橱列表视图
筛选抽屉
保存搭配弹层
更新原搭配 / 另存为新搭配弹层
删除确认等业务弹层
```

如果某个子页面以后获得用户单独确认的设计图，则新设计图优先于本规范。

---

# 2. 视觉来源

子页面不得创造新的设计语言，统一继承三张主页面参考图：

- 粉白 / 奶白背景。
- 粉色作为主强调色。
- 大圆角卡片。
- 浅粉或米色细边框。
- 柔和低强度阴影。
- 深灰主文字、暖灰次文字。
- 充足留白。
- 胶囊按钮和标签。
- 衣物图片以完整显示主体为优先，使用等效 `contain`。

禁止：深色后台风、Material 风、强渐变、大面积纯黑、斜体、复杂玻璃拟态、不同页面各用一套配色。

---

# 3. 子页面统一页面骨架

非 Tab 子页面统一采用：

```text
顶部导航区
→ 页面标题 / 必要说明
→ 主内容区
→ 必要底部操作区
```

顶部返回行为优先使用微信小程序现有导航机制和项目已有导航组件。

不得为了视觉效果隐藏用户无法返回页面的入口。

---

# 4. 列表类页面

适用于：

- 匹配穿搭结果。
- 历史搭配选择。
- 我的搭配。

统一规则：

- 卡片样式继承首页「最近保存穿搭」。
- 穿搭缩略图使用叠穿平铺预览。
- 卡片展示名称、季节、风格、日期等必要信息。
- 当前页面标题下可显示筛选条件 / 数量等辅助信息。
- 列表卡点击统一进入穿搭详情，除开发文档明确规定的其他动作外，不在卡片上堆叠大量按钮。

空状态使用同一结构：

```text
轻量图标 / 插画
主提示
一行辅助说明
一个主要下一步按钮（如确有需要）
```

---

# 5. 详情类页面

适用于：

- 穿搭详情。
- 单品详情。

统一结构：

```text
顶部返回 + 标题
→ 大尺寸主图 / 预览
→ 基础信息卡
→ 补充信息卡（存在时）
→ 页面底部主要操作
```

主要操作使用粉色主按钮；危险操作「删除」使用弱化样式并二次确认，不与主操作争夺视觉焦点。

只读详情页不允许直接在字段中编辑；需要修改时进入明确编辑状态 / 编辑页。

---

# 6. 编辑类页面

适用于：

- 单品编辑。
- 穿搭编辑载入后的保存信息。

统一：

- 字段分组使用白色 / 浅粉圆角卡片。
- 必填项用统一 `*` 或项目已有必填表现。
- 标签选择沿用衣橱与首页的粉色胶囊选中态。
- 不使用密集后台表格风格。
- 表单校验在对应字段附近给出简短提示。

---

# 7. 抽屉与弹层

适用于：

- 衣橱筛选。
- 保存搭配。
- 更新原搭配 / 另存为新搭配。
- 删除确认。

统一：

- 优先底部圆角抽屉或居中轻量确认框，按现有工程组件决定。
- 背景遮罩保持轻量，不做重度模糊。
- 主按钮粉色；次按钮浅底。
- 危险删除按钮只在确认删除场景使用警示色 / 项目已有危险样式。
- 弹层内容只包含完成当前动作所需字段，不增加额外产品功能。

保存搭配弹层固定字段：

```text
搭配名称
季节
风格
```

衣橱筛选抽屉固定字段：

```text
季节
风格
主颜色
厚度
```

---

# 8. 城市选择

城市选择只承载已确认功能：

```text
重新定位当前位置
手动搜索 / 选择城市
```

建议结构沿用主页面圆角输入框 / 卡片视觉：

```text
顶部返回 + 城市选择
→ 搜索框
→ 重新定位入口
→ 搜索结果 / 可选城市列表
```

不得擅自增加天气预报大页面、地图选点或城市收藏等 V1 未确认功能。

---

# 9. 个人中心

当前产品只确认「头像点击进入个人中心」，尚未确认个人中心具体业务模块。

因此：

- 如果现有工程已有个人中心，复用并只做必要视觉对齐。
- 如果现有工程没有个人中心，不得由子代理自行新增账号设置、会员、社交、商城等功能。
- 开发遇到必须新增的个人中心内容时，列为待确认项交给用户，不自行设计产品功能。

---

# 10. 统一交互状态

所有子页面都必须覆盖：

- 加载中。
- 空状态。
- 请求失败。
- 保存 / 删除失败。

失败不得整页白屏，不得清空用户尚未提交的输入或选衣结果。

---

# 11. 微信小程序适配

- 使用现有小程序导航和组件体系。
- 注意状态栏、导航栏及底部安全区。
- 主要点击区域满足手机触控需要，不做过小图标按钮。
- 长列表使用微信小程序现有滚动能力，不引入 Web DOM 方案。
- 底部操作区不得与系统安全区或 TabBar 重叠。

---

# 12. 子页面视觉验收

- [ ] 与首页 / 衣橱 / 穿搭属于同一套粉白视觉语言。
- [ ] 没有自行引入新的主色、字体和卡片体系。
- [ ] 列表类页面卡片风格统一。
- [ ] 详情类页面主图与信息层级统一。
- [ ] 弹层按钮层级统一。
- [ ] 空 / 错 / 加载状态风格统一。
- [ ] 未经用户确认没有新增产品功能。
