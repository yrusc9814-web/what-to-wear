# 小衣橱 V1.4｜资料审计收口说明

状态：已完成文档级收口

## 已修复

1. `小衣橱-agent.md` 已正式更名为根目录 `AGENTS.md`。
2. 全部正式文档引用统一到 V1.4，并统一为资料包中的真实相对路径。
3. 今日穿搭改为唯一事实源 `todayOutfit = { date, outfitId } | null`，删除 `SavedOutfit.isToday` 双状态设计。
4. 跨日规则固定：进入下一天后，昨日今日穿搭失效，当前变为“没有今日穿搭”。
5. 历史穿搭图片生命周期闭环：衣橱删除只做业务 / 逻辑删除；被历史穿搭引用的云端图片不得同时物理删除。
6. 主颜色正式枚举固定，并统一为单品单选 / 衣橱筛选单选。
7. 当前季节固定为：3～5 月春、6～8 月夏、9～11 月秋、12～2 月冬；首页默认筛选为当前季节 + 风格不限。
8. 新增《子页面统一视觉规范》，覆盖没有独立设计图的列表、详情、弹层、城市选择等子页面。
9. 新增《00-开发资料索引》，统一实际文件路径与必读顺序。
10. 补充微信用户数据隔离要求：衣橱、穿搭、今日穿搭、偏好必须按现有 OpenID / userId 作用域读取。
11. 补充未保存穿搭草稿规则：正常 Tab 切换不得清空；加载另一套编辑前必须提示是否覆盖脏草稿。
12. 清理 macOS `.DS_Store` / `__MACOSX` 杂项，不放入正式资料包。

## 仍需在真实源码开工前检查

以下不是产品文档缺口，而是需要 Codex 主代理读取真实微信小程序源码后确认的工程现状：

- 当前 OpenID / userId 的实际实现位置与数据库作用域。
- 当前 AI 识图服务、响应字段、API Key 安全存放方式。
- 当前天气 / 城市 API 与定位封装。
- 当前云存储 `fileId`、上传、替换、删除代码。
- 当前穿搭草稿是否已有本地 / 云端持久化。
- 当前 `app.json`、TabBar、页面路由、云函数和历史数据结构。

这些内容必须“先读现有代码再适配”，不能仅凭文档猜测。

## 文档级结论

当前资料已经可以冻结为 **V1.4 正式开发基线**。

在真实源码完成开工前检查后，可以按公共边界启动首页 / 衣橱 / 穿搭三个子代理并行开发。
