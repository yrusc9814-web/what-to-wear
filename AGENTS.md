# 小衣橱｜AGENTS.md

## 1. 项目定位

本项目是 **微信小程序**「小衣橱」。

核心用途：

```text
录入和管理个人衣橱
→ 手动搭配单品
→ 保存穿搭
→ 查看历史穿搭
→ 设置今日穿搭
→ 根据季节 / 风格筛选已保存穿搭
```

V1 底部固定 3 个主栏目：

```text
首页
衣橱
穿搭
```

不得新增第四个 Tab。

---

## 2. 开发依据与优先级

发生冲突时按以下优先级执行：

```text
1. 用户最新明确要求
2. 对应栏目最新开发文档
3. 《三子代理公共接口与开发边界说明》
4. 最新正式 PRD
5. 现有代码行为
```

资料总索引：`00-开发资料索引.md`

当前栏目开发文档：

```text
第一栏/小衣橱-首页开发文档-V1.md
第二栏/小衣橱-衣橱开发文档-V1.md
第三栏/小衣橱-穿搭开发文档-V1.md
小衣橱-三子代理公共接口与开发边界说明-V1.md
小衣橱-子页面统一视觉规范-V1.md
小衣橱_V1_正式PRD_V1.4.md
```

旧 PRD / 旧页面 / 旧代码只能用于兼容参考，不得覆盖已经确认的新规则。

---

## 3. 开发前强制检查

任何修改前必须先：

```text
1. 检查当前 Git 状态
2. 阅读本文件
3. 阅读本任务对应的栏目开发文档
4. 检查现有小程序目录、页面、组件、云函数和数据结构
5. 搜索是否已有可复用实现
6. 列出计划修改文件
7. 确认不会覆盖其他代理正在负责的范围
8. 再开始修改
```

主代理在并发派发前还必须确认现有工程中的：

```text
微信用户身份 / OpenID 数据隔离方式
AI 识图服务与密钥存放方式
天气 / 城市服务与现有定位封装
图片云存储 fileId / 删除生命周期
穿搭草稿当前是否已有持久化逻辑
```

上述内容优先复用现有实现；不得让三个子代理各自重做一套。

不得先假设工程结构再写代码。

不得为了“更干净”直接大规模重构现有工程。

---

## 4. 微信小程序技术约束

这是微信小程序，不是普通 Web 项目。

开发必须遵循当前工程已有技术栈。

### 必须

- 使用现有小程序页面 / 组件体系。
- 页面注册、TabBar、路由遵循现有 `app.json` 等配置。
- Tab 页面之间使用适合微信小程序的 Tab 跳转方式。
- 非 Tab 子页面使用现有正式导航方式。
- 使用微信小程序支持的布局、事件和生命周期。
- 样式优先沿用项目已有 `rpx` / WXSS 体系。
- 适配常见手机屏幕和底部安全区。
- 图片展示衣物主体时使用等效 `contain` 逻辑，不裁掉主体。
- 拍照、相册、定位、存储、上传等能力优先复用项目已有封装。

### 禁止

- 引入浏览器 DOM 操作。
- 使用 `document` / `window` 作为正式实现。
- 使用 Web `localStorage` 替代现有小程序存储。
- 照搬网页原型事件代码。
- 因为方便而把项目迁移成 React / Vue / H5。
- 未经要求更换现有小程序技术栈。

如果现有项目使用 JS，则继续 JS；如果使用 TS，则继续 TS。不得擅自迁移语言。

---

## 5. 当前工程保护范围

必须保留现有：

- 微信小程序工程主体。
- 三 Tab 架构。
- 首页天气能力。
- 衣橱上传和管理能力。
- 图片云存储通路。
- 穿搭保存、查询、删除能力。
- 历史记录兼容逻辑。

重点调整现有穿搭 / 推荐相关页面时，应先检查：

```text
miniprogram/pages/recommendation/
```

不得通过回滚旧提交覆盖当前工作区。

不得破坏未提交修改。

---

## 6. 云函数与数据兼容

现有云函数和数据库属于高风险共享区域。

### 强制规则

- 不得擅自修改现有云函数 `TYPES`。
- 不得删除旧数据兼容逻辑。
- 不得随意重命名已有数据库字段。
- 新增字段前先检查旧数据迁移和兼容性。
- 公共数据模型修改由主代理统一处理。
- 子代理不得各自创建互不兼容的数据结构。

如果现有数据结构与开发文档不同：

```text
优先做兼容适配
不要直接破坏旧数据
```

---

## 6.1 用户数据隔离

所有衣橱、搭配、今日穿搭和用户偏好必须按当前微信用户身份隔离。

- 优先复用现有 OpenID / userId / session 机制。
- 所有读写必须带用户作用域。
- 禁止查询或写入其他用户的数据。
- 用户身份字段属于共享数据契约，由主代理统一处理。

## 7. 公共分类

正式单品只有：

```text
帽子
上衣
下装
鞋子
包包
```

统一值：

```text
hat
top
bottom
shoes
bag
```

禁止新增正式：

```text
配饰
accessory
```

旧数据如存在，只做兼容。

---

## 8. 公共季节与风格

季节统一：

```text
spring
summer
autumn
winter
```

不得混用 `fall`。

风格统一：

```text
casual
commute
sweet
cool
```

中文：

```text
休闲
通勤
甜美
潮酷
```

`all / 不限` 只允许作为筛选条件，不得写入正式搭配风格数据。

---

## 8.1 主颜色统一枚举

V1 主颜色固定：

```text
粉色 / 白色 / 黑色 / 米色 / 灰色 / 蓝色 / 棕色 / 绿色 / 红色 / 黄色 / 紫色 / 多色 / 其他
```

单品主颜色单选，衣橱主颜色筛选单选。不得由子代理新增另一套颜色枚举。

## 8.2 当前季节统一规则

统一按用户设备本地月份：

```text
3～5 月 = 春
6～8 月 = 夏
9～11 月 = 秋
12～2 月 = 冬
```

首页默认季节与穿搭「换一套」必须复用同一公共函数。首页默认风格为「不限」。

## 9. 衣橱规则

手机端默认：

```text
两列网格
```

保留：

```text
两列网格 / 列表视图切换
```

V1 筛选：

```text
季节：单选
风格：单选
主颜色：单选
厚度：单选
```

单品自身：

```text
季节：可多选
风格：可多选
主颜色：单选
厚度：单选
```

不要混淆“单品字段”和“筛选条件”。

---

## 10. 上传与 AI 识别

上传新单品固定三步：

```text
1. 上传图片
2. AI 识别信息
3. 确认保存
```

AI 可以建议：

- 单品名称
- 分类
- 主颜色
- 适用季节
- 风格
- 厚度
- AI 单品介绍

AI 不得根据图片编造：

- 购买价格
- 购买日期
- 购买渠道
- 尺码
- 品牌
- 真实材质成分

AI 结果必须允许用户确认和修改。

AI 失败时必须允许用户手动填写并继续保存。

---

## 11. 单品删除规则

删除衣橱单品采用历史快照兼容方案。

删除后：

```text
衣橱：不再显示
新搭配：不可再选择
换一套：不可再使用
历史搭配：继续显示保存时快照
历史编辑：提示重新选择该槽位
```

不得因为删除一件衣服导致历史穿搭丢图或损坏。

若图片仍被历史穿搭 / 今日穿搭引用，云端文件不得物理删除；只做逻辑删除，待无正式引用后才能垃圾回收。

---

## 12. 穿搭五槽

固定：

```text
帽子
上衣
下装
鞋子
包包
```

必选：

```text
上衣
下装
鞋子
```

可选：

```text
帽子
包包
```

帽子和包包允许为空。

不得新增其他槽位。

---

## 13. 穿搭预览

顶部预览必须是 **叠穿平铺组合**。

正确关系：

```text
帽子在上方
上衣在上半身
下装与上衣自然衔接 / 轻微重叠
鞋子在下方
包包可覆盖侧边
```

禁止：

- 真人试穿。
- 人体模型。
- 人体轮廓。
- AI 生成新的服装图片。
- 五张商品图简单并排。
- 针对某一件具体衣服写硬编码坐标补丁。

布局应使用统一槽位规则。

---

## 14. 穿搭页面入口

直接点击底部：

```text
穿搭
```

进入完整穿搭主页面。

首页点击：

```text
快速开始搭配
```

则：

```text
先展示历史保存组合
→ 不自动选中
→ 用户可进入详情
```

不满意时：

```text
去搭配单品
→ 穿搭主页面
```

不得把两个入口逻辑混为一谈。

---

## 14.1 未保存草稿

正常切换 Tab 不得清空穿搭草稿。

从详情点击「编辑」将另一套搭配加载到编辑器时，如当前已有未保存草稿，必须先提示确认是否替换；未确认不得覆盖。

## 15. 换一套

「换一套」规则固定：

```text
根据当前季节
+
风格不限
+
从当前有效衣橱单品中随机组合
```

至少包含：

```text
上衣
下装
鞋子
```

帽子 / 包包可无。

不得：

- 使用已删除单品。
- 生成新衣服。
- 自动保存。
- 自动设为今日穿搭。

当前季节判断必须复用统一公共规则。

---

## 16. 保存搭配

保存前要求：

```text
搭配名称：必填
季节：单选必填
风格：单选必填
```

核心槽位：

```text
上衣 / 下装 / 鞋子
```

必须完整。

保存成功后：

```text
停留在穿搭页
```

不得自动跳首页。

---

## 17. 编辑历史搭配

所有历史搭配点击后：

```text
先进入穿搭详情
```

用户进一步点击：

```text
编辑
```

才加载到穿搭主页面。

编辑后保存必须提供：

```text
更新原搭配
另存为新搭配
取消
```

不得强制覆盖。

---

## 18. 今日穿搭

匹配、历史列表、换一套都不得自动设置今日穿搭。

唯一正式流程：

```text
穿搭详情
→ 用户点击「设为今日穿搭」
→ 用户确认
→ todayOutfit = { 当日本地日期, outfitId }
```

`todayOutfit` 是唯一事实源；不得同时维护 `SavedOutfit.isToday`。

跨日规则：

```text
进入下一天
→ 昨日今日穿搭失效
→ 当前变成“没有今日穿搭”
```

小程序启动 / 回到前台 / 首页读取时按设备本地日期校验，无需后台定时器。
---

## 19. 首页智能筛选

首页智能穿搭只允许：

```text
从用户已保存穿搭中筛选
```

筛选：

```text
季节：单选
风格：单选
风格支持“不限”
```

禁止临时从衣橱生成新穿搭作为匹配结果。

匹配结果：

```text
列表
→ 点击某套
→ 穿搭详情
→ 再决定编辑或设为今日穿搭
```

---

## 20. 首页城市和天气

城市支持：

```text
重新定位
+
手动选择城市
```

城市改变后必须同步更新：

- 城市
- 当前温度
- 天气状态
- 天气图标
- 温度区间
- 天气建议

天气失败不得阻塞首页其他模块。

---

## 21. 视觉总规则

设计图是视觉最高依据。

整体保持：

- 女性化、轻盈。
- 浅粉 / 奶白。
- 大圆角。
- 轻描边。
- 柔和阴影。
- 充足留白。
- 粉色选中态。
- 中文系统字体 / 项目既有字体。
- 禁止斜体。

开发时：

```text
优先复刻参考图的信息层级、比例、留白和卡片关系
```

不得以“优化”为理由擅自重排页面。

没有单独参考图的子页面统一执行 `小衣橱-子页面统一视觉规范-V1.md`，不得各自发挥成不同风格。

---

## 22. 三子代理并发边界

### 首页子代理

只负责：

- 首页四模块
- 天气 / 城市
- 最近保存
- 智能筛选
- 首页跳转和状态

### 衣橱子代理

只负责：

- 衣橱
- 两列网格 / 列表
- 搜索 / 筛选
- 上传三步
- AI 识别
- 单品详情 / 编辑 / 删除

### 穿搭子代理

只负责：

- 穿搭主页面
- 历史搭配选择
- 我的搭配
- 穿搭详情
- 五槽
- 换一套
- 保存 / 编辑
- 今日穿搭入口

不得越权重做其他栏目。

---

## 23. 共享文件规则

以下为高冲突共享文件 / 模块：

- `app.json`
- TabBar 配置
- 公共类型
- 公共枚举
- 公共 Store / 状态
- 公共 API
- 数据库 schema
- 云函数公共常量
- 路由公共协议
- 季节判断工具
- `SavedOutfit / WardrobeItem` 数据模型

三个子代理不得同时直接修改。

需要修改时：

```text
子代理汇报需求
→ 主代理统一修改
→ 子代理再适配
```

---

## 24. Git 与文件安全

禁止：

- `git reset --hard`
- 强制覆盖用户未提交修改
- 恢复整个旧 commit 覆盖当前工程
- 删除不属于本任务的文件
- 随意清理工作区

修改前必须检查 `git status`。

只修改完成当前任务真正需要的文件。

---

## 25. 实现原则

优先级：

```text
正确业务
→ 数据兼容
→ 视觉对齐
→ 交互完整
→ 代码质量
→ 性能优化
```

但性能优化不得改变已确认交互。

禁止为了快速通过：

- 假数据永久兜底。
- 空点击事件。
- TODO 代替正式实现。
- 隐藏错误。
- 用错误图片替代加载失败图片。
- 硬编码单件衣服位置。

---

## 26. 测试要求

每个子代理完成后至少自检：

- 页面可正常进入。
- 无明显运行错误。
- 关键操作流可执行。
- 空状态正确。
- 失败状态不白屏。
- 修改没有破坏其他两个 Tab。
- 数据保存后刷新仍存在。
- 历史数据仍可读取。

主代理最终必须做跨栏目联调。

---

## 27. 跨栏目最终门禁

必须验证：

```text
衣橱新增
→ 穿搭可选

衣橱删除
→ 新搭配不可选
→ 历史穿搭仍正常

穿搭保存
→ 首页最近保存出现

设置今日穿搭
→ 首页刷新

首页智能筛选
→ 正确找到已保存搭配

首页快速开始
→ 历史组合
→ 去搭配单品
→ 穿搭主页面
```

未完成联调不得宣布项目完成。

---

## 28. 遇到不明确需求

如果：

- 开发文档没有定义。
- 图片无法判断。
- 多种实现会改变用户操作流。
- 涉及新增产品功能。

必须：

```text
停止对该点自行设计
→ 明确列出问题
→ 等用户确认
```

不得自作主张补产品需求。

纯技术实现细节如果不改变用户体验、数据和布局，可按现有工程最佳兼容方式处理。

---

## 29. 完成汇报格式

子代理完成后只汇报：

```text
1. 修改了什么
2. 修改文件
3. 自测结果
4. 是否存在未完成 / 阻塞
5. 是否触碰共享文件
```

不要长篇解释实现过程。

主代理负责最终汇总和验收。
