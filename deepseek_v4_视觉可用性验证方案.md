# DeepSeek V4 视觉可用性验证方案

适用目标：验证你当前持有的 DeepSeek API，是否适合用于“穿搭日记”微信小程序的衣物图片识别场景。

## 1. 先判断什么算“能用”

这个项目不是普通聊天，而是一个偏产品化的图像识别任务。可用标准不是“偶尔识别对”，而是：

- 能接收单张衣物图片输入
- 能稳定返回结构化 JSON
- 能识别单件衣物的主类别、颜色、季节、风格等基础标签
- 对挂拍、平铺、白底、室内光线变化有一定鲁棒性
- 8 到 10 秒内能完成一次识别
- 失败时能明确报错，不是长时间卡住

如果只满足“能看图聊天”，但不满足“稳定结构化输出”，对这个小程序也不够。

## 2. 验证结论分级

### A 级：可以作为第一版主方案

- 图片输入接口可用
- 10 张测试图中至少 8 张返回合法 JSON
- 主类别准确率 >= 80%
- 主颜色准确率 >= 70%
- 季节/风格标签大体可用，允许少量偏差
- 平均响应时间 <= 8 秒

### B 级：可以做备选或灰度验证

- 图片输入接口可用
- 10 张测试图中至少 6 张返回合法 JSON
- 主类别准确率 >= 60%
- 经常需要手动修正，但能给出有参考价值的初稿

### C 级：不建议写进第一版主链路

- 图片接口本身不可用
- 经常不返回 JSON
- 把背景、模特、配饰误判成主要衣物
- 响应过慢，超过 10 秒较多
- 同一张图多次识别结果飘得很厉害

## 3. 推荐测试集

先不要追求很多图，先准备 8 到 10 张就够判断方向。

- 2 张上衣：T 恤、衬衫
- 2 张下装：半裙、牛仔裤
- 2 张鞋子：运动鞋、单鞋
- 1 张帽子
- 1 张边界图：光线一般、背景复杂
- 1 张非服装图：包、杯子、人物自拍、商品拼图任选其一

图片要求：

- 尽量使用你未来真实用户会上传的图片风格
- 不要全是电商白底图
- 至少包含 2 张“普通用户随手拍”

## 4. 每张图要记录什么

建议手工建一个表，字段如下：

- image_name
- expected_category
- expected_color
- expected_season
- actual_category
- actual_color
- actual_season
- json_valid
- latency_ms
- result_notes

其中 `result_notes` 重点记：

- 是否把衣架/背景误识别进去了
- 是否输出了你不需要的废话
- 是否漏字段
- 是否把“鞋子”误成“上衣”这类严重错误

## 5. 建议的输出结构

你这个项目里可以先按下面这套字段验，不必一步到位做太复杂：

```json
{
  "category": "top",
  "subCategory": "tshirt",
  "color": "white",
  "seasonTags": ["spring", "summer"],
  "styleTags": ["casual", "minimal"],
  "fitTags": ["regular"],
  "confidence": 0.82
}
```

判断重点：

- `category` 是否稳定
- `color` 是否基本靠谱
- `styleTags` 是否有参考价值
- `confidence` 是否和实际效果大致一致

## 6. 推荐执行顺序

### 第一步：接口烟雾测试

先验证你的 API 是否接受图片输入。

如果接口直接报 schema 错误、`image_url` 不支持、或只接受纯文本，那就可以初步判定：

- 这条 DeepSeek API 不适合作为当前视觉主方案

### 第二步：结构化输出测试

如果图片输入可用，再验证：

- 能否稳定输出合法 JSON
- 会不会夹带解释文字
- 字段是否经常缺失

### 第三步：识别质量测试

重点看：

- 主类别
- 主颜色
- 季节
- 风格标签

其中第一版最重要的是：

- 类别别错太多
- 颜色别太离谱

风格标签可以允许偏松一些，因为本来就带主观性。

### 第四步：稳定性测试

同一张图连续跑 3 次，看结果是否明显漂移。

如果连续三次：

- `category` 一致
- `color` 基本一致
- `styleTags` 只有轻微变化

说明稳定性还可以。

## 7. 对“穿搭日记”这个项目的建议阈值

如果你只是做 MVP，我建议把可用门槛定得现实一点：

- `category` 准确率 >= 80%
- `color` 准确率 >= 70%
- 非服装图能较高概率提示低置信度或识别失败
- 单次识别平均耗时 <= 8 秒

只要达到这个水平，再加上“用户可手动修改”，其实就能先跑起来。

## 8. 失败时怎么解释结果

### 情况 1：接口不支持图片

结论：

- 你的这条 DeepSeek API 当前不能直接承担衣物视觉识别
- PRD 不要把 DeepSeek V4 写成主视觉模型

### 情况 2：支持图片，但识别一般

结论：

- 可以作为实验方案
- 不建议直接写死为第一版唯一方案

### 情况 3：支持图片且结果稳定

结论：

- 可以把 PRD 改成“默认接入 DeepSeek”
- 但仍建议保留“可切换供应商”的技术方案表述

## 9. 仓库内附带脚本

本仓库已提供脚本：

- [validate_deepseek_v4_for_clothing.py](D:\chuanda-project\validate_deepseek_v4_for_clothing.py)

用途：

- 直接验证图片输入接口是否可用
- 让模型按衣物识别场景输出 JSON
- 记录响应时间
- 输出每张图的识别结果

## 10. 建议你实际怎么跑

1. 先挑 3 张图做烟雾测试
2. 如果图片输入能通，再补到 8 到 10 张
3. 我们再根据结果决定：
   - 改 PRD 为 DeepSeek 主方案
   - 或改成厂商中立方案

