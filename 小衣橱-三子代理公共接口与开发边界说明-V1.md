# 小衣橱｜三子代理公共接口与开发边界说明 V1

版本：V1.1（收口版）  
用途：供 Codex 主代理在并发启动「首页 / 衣橱 / 穿搭」3 个子代理前统一下发  
配套文档：

- `第一栏/小衣橱-首页开发文档-V1.md`
- `第二栏/小衣橱-衣橱开发文档-V1.md`
- `第三栏/小衣橱-穿搭开发文档-V1.md`
- `小衣橱_V1_正式PRD_V1.4.md`
- `小衣橱-子页面统一视觉规范-V1.md`
- `AGENTS.md`

---

# 1. 目的

三个子代理可以并行开发，但必须共享同一套：

```text
路由协议
数据字段
单品分类
季节 / 风格枚举
穿搭结构
今日穿搭状态
历史快照规则
页面跳转参数
```

本文件用于避免：

- 三个代理各自定义一套 `Outfit`。
- 首页和穿搭使用不同季节字段。
- 衣橱删除单品后穿搭 / 首页失效。
- 同一路由使用不同参数名。
- 多个子代理同时修改同一个公共文件造成冲突。
- 联调阶段再大规模返工。

---

# 2. 并发开发总原则

Codex 主代理负责：

```text
统一公共协议
→ 分派 3 个子代理
→ 控制共享文件修改
→ 最终合并
→ 联调验收
```

三个子代理分别负责：

```text
子代理 A：首页
子代理 B：衣橱
子代理 C：穿搭
```

三个子代理不得擅自修改其他栏目已经明确冻结的业务规则。

---

# 3. 文档优先级

遇到冲突时按以下顺序执行：

```text
1. 对应栏目最新开发文档
2. 本公共接口与边界说明
3. V1.4 正式 PRD
4. 子页面统一视觉规范（涉及子页面视觉时）
5. 现有代码实现
```

如果现有代码与开发文档冲突：

```text
不得偷偷沿用旧逻辑
→ 报告冲突
→ 按最新开发文档整改
```

---

# 4. 页面正式名称

底部固定三个栏目：

```text
首页
衣橱
穿搭
```

正式名称统一为：

```text
首页
我的衣橱
穿搭
```

不得继续使用：

```text
AI搭配
穿搭平铺
```

作为正式页面标题。

---

# 5. 正式单品分类

全项目只使用：

```ts
type WardrobeCategory =
  | 'hat'
  | 'top'
  | 'bottom'
  | 'shoes'
  | 'bag'
```

中文映射：

```text
hat     → 帽子
top     → 上衣
bottom  → 下装
shoes   → 鞋子
bag     → 包包
```

V1 禁止正式使用：

```text
配饰
accessory
```

如果旧数据存在配饰：

```text
只做历史兼容
不得继续作为新数据分类写入
```

---

# 6. 季节枚举

统一使用：

```ts
type Season =
  | 'spring'
  | 'summer'
  | 'autumn'
  | 'winter'
```

中文：

```text
spring → 春
summer → 夏
autumn → 秋
winter → 冬
```

不得某个模块使用：

```text
fall
```

另一个模块使用：

```text
autumn
```

本项目统一：

```text
autumn
```

---

# 7. 风格枚举

统一使用：

```ts
type OutfitStyle =
  | 'casual'
  | 'commute'
  | 'sweet'
  | 'cool'
```

中文：

```text
casual  → 休闲
commute → 通勤
sweet   → 甜美
cool    → 潮酷
```

首页筛选额外允许：

```ts
'all'
```

但：

```text
all / 不限
```

只属于筛选条件。

禁止把 `all` 保存到：

- 单品风格。
- 搭配风格。
- 数据库正式风格值。

---

# 7.1 主颜色枚举

V1 正式主颜色使用单值枚举：

```ts
type PrimaryColor =
  | 'pink'
  | 'white'
  | 'black'
  | 'beige'
  | 'gray'
  | 'blue'
  | 'brown'
  | 'green'
  | 'red'
  | 'yellow'
  | 'purple'
  | 'multicolor'
  | 'other'
```

中文映射：

```text
pink       → 粉色
white      → 白色
black      → 黑色
beige      → 米色
gray       → 灰色
blue       → 蓝色
brown      → 棕色
green      → 绿色
red        → 红色
yellow     → 黄色
purple     → 紫色
multicolor → 多色
other      → 其他
```

规则：

- 单品 `primaryColor` 只能保存一个值。
- 衣橱主颜色筛选也为单选。
- AI 无法可靠判断时可留空或建议 `other`，不得强行猜色。
- 如现有历史数据存在其他颜色字符串，由主代理做兼容映射，不要求破坏历史数据。

---

# 7.2 用户数据隔离

这是微信小程序的个人衣橱数据，所有用户自有数据必须按当前微信用户身份隔离。

需要隔离的至少包括：

```text
WardrobeItem
SavedOutfit
todayOutfit
城市 / 用户偏好
未保存穿搭草稿（如果持久化）
```

实现规则：

- 优先复用现有工程的 OpenID / userId / session 用户标识。
- 所有查询、更新、删除必须带当前用户作用域，禁止读取全局其他用户记录。
- 不得由三个子代理各自发明不同用户 ID 字段；公共身份字段由主代理统一适配现有工程。
- 如果现有工程尚未做用户隔离，主代理必须在三代理正式写数据前先补齐公共数据作用域。

---

# 8. 衣橱单品公共结构

建议统一语义：

```ts
type WardrobeItem = {
  id: string

  createdAt: string
  updatedAt: string

  imageUrl: string
  fileId?: string

  name: string
  category: WardrobeCategory

  seasons: Season[]
  styles: OutfitStyle[]

  primaryColor?: PrimaryColor
  thickness?: 'thin' | 'medium' | 'thick'
  size?: string

  purchasePrice?: number | null
  purchaseDate?: string
  purchaseChannel?: string

  aiDescription?: string
  note?: string

  deletedAt?: string | null
}
```

原则：

- `category` 单值。
- `primaryColor` 单值。
- 单品的 `seasons` 可以多选。
- 单品的 `styles` 可以多选。
- `deletedAt != null` 的单品不得参与新搭配。

如现有数据库字段不同：

```text
优先适配现有数据结构
```

但业务语义必须保持一致。

---

# 9. 衣橱筛选与单品字段不要混淆

单品自身：

```text
季节：可多选
风格：可多选
主颜色：单选
厚度：单选
```

衣橱筛选面板：

```text
季节：单选
风格：单选
主颜色：单选
厚度：单选
```

首页智能穿搭筛选：

```text
季节：单选
风格：单选
风格允许“不限”
```

三个场景不能混成同一套交互规则。

---

# 10. 搭配槽位公共结构

全项目统一五槽：

```ts
type OutfitSlots = {
  hat: OutfitItemSnapshot | null
  top: OutfitItemSnapshot
  bottom: OutfitItemSnapshot
  shoes: OutfitItemSnapshot
  bag: OutfitItemSnapshot | null
}
```

必选：

```text
上衣
下装
鞋子
```

可选：

```text
帽子
包包
```

不得新增：

```text
配饰槽
首饰槽
袜子槽
```

V1 暂不做。

---

# 11. 历史快照协议

保存搭配时不能只保存 `itemId`。

每个槽位至少需要保存：

```ts
type OutfitItemSnapshot = {
  itemId: string | null

  snapshot: {
    name: string
    category: WardrobeCategory
    imageUrl: string
    imageFileId?: string
    primaryColor?: PrimaryColor
  }
}
```

目的：

```text
即使衣橱原单品之后被删除
历史搭配仍然可以正常展示
```

衣橱删除单品后：

```text
新搭配：不可再使用
历史搭配：继续展示快照
历史编辑：提示重新选择有效单品
```

三个子代理必须统一遵守。

---

# 11.1 图片引用与物理删除生命周期

历史快照能够继续显示的前提是：**被历史穿搭引用的云端图片不能被衣橱删除动作同时物理删除。**

正式规则：

```text
衣橱删除单品
→ 先做逻辑删除 / 业务删除
→ 从新搭配候选中移除
→ 检查该单品图片是否仍被任一历史穿搭快照引用
```

如果仍被历史穿搭引用：

```text
保留云端原图 / fileId
不得物理删除
```

只有确认没有任何历史穿搭、今日穿搭或其他正式记录引用时，才允许后续垃圾回收物理文件。

快照应优先持久化稳定的 `imageFileId`（或现有云存储等价标识），同时保留展示所需 `imageUrl`。如果现有工程只有 URL，也必须保证底层文件在有历史引用期间不被删除。

V1 不要求立即实现复杂引用计数系统，但必须满足最终业务结果：

```text
删除衣橱单品 ≠ 删除仍被历史穿搭使用的图片文件
```

---

# 12. 已保存搭配公共结构

建议语义：

```ts
type SavedOutfit = {
  id: string

  createdAt: string
  updatedAt: string

  title: string

  season: Season
  style: OutfitStyle

  items: {
    hat: OutfitItemSnapshot | null
    top: OutfitItemSnapshot
    bottom: OutfitItemSnapshot
    shoes: OutfitItemSnapshot
    bag: OutfitItemSnapshot | null
  }

  previewImageUrl?: string
  previewFileId?: string
}
```

要求：

- `title` 必填。
- `season` 单选。
- `style` 单选。
- 首页智能筛选直接读取 `season/style`。
- 预览图只负责展示。
- `items` 才是结构化主数据。

---

# 13. 今日穿搭状态

今日穿搭使用**单一事实源**，不得同时维护 `SavedOutfit.isToday` 和另一个全局 ID。

统一语义：

```ts
type TodayOutfitAssignment = {
  date: string      // YYYY-MM-DD，按用户设备本地日历日期
  outfitId: string
} | null
```

全局仅保存：

```ts
todayOutfit: TodayOutfitAssignment
```

不得再在 `SavedOutfit` 中维护 `isToday` 作为第二事实源。

设置流程：

```text
穿搭详情
→ 用户点击「设为今日穿搭」
→ 用户确认
→ todayOutfit = { 当日本地日期, outfitId }
```

跨日规则已确认：

```text
日期进入下一天
→ 昨日 todayOutfit 不再有效
→ 当前状态视为「没有今日穿搭」
```

实现无需后台定时器。微信小程序启动、回到前台或首页读取今日穿搭时，比较 `todayOutfit.date` 与当前设备本地日期：

```text
相同 → 正常读取
不同 → 清空或按空值处理
```

禁止：

- 首页匹配后自动设置。
- 点击历史卡自动设置。
- 点击「换一套」自动设置。
- 跨日后继续把昨日搭配显示为“今日穿搭”。
---

# 13.1 穿搭草稿生命周期

V1 保留未保存穿搭草稿，避免普通 Tab 切换导致用户选择丢失。

规则：

```text
穿搭主页面产生未保存修改
→ dirty = true
→ 切换首页 / 衣橱再返回
→ 保留当前草稿
```

从穿搭详情点击「编辑」需要加载另一套搭配时，如果当前已经存在 `dirty = true` 的草稿：

```text
先提示当前未保存搭配将被替换
→ 用户确认后再加载
→ 用户取消则保留现有草稿
```

首页「快速开始搭配」进入历史组合选择本身不得覆盖草稿；只有用户在详情中明确点击「编辑」并确认替换时才允许写入编辑器。

小程序冷启动后的草稿是否持久化，优先复用现有工程能力；如没有持久化机制，V1 最低要求是在本次小程序运行期间和正常 Tab 切换期间不丢失。

---

# 14. 页面入口协议

## 14.1 底部 Tab「穿搭」

```text
直接进入穿搭主页面
```

即参考图中的：

```text
穿搭预览
+ 五类单品选择
+ 换一套
+ 保存搭配
```

## 14.2 首页「快速开始搭配」

不是直接进入五类编辑器。

流程：

```text
首页快速开始搭配
→ 历史搭配选择
→ 不自动选中
```

用户不满意：

```text
去搭配单品
→ 穿搭主页面
```

---

# 15. `outfitId` 统一协议

所有查看既有穿搭的入口统一携带：

```text
outfitId
```

包括：

- 首页最近保存。
- 首页匹配结果。
- 我的搭配。
- 历史搭配选择。

禁止出现：

```text
首页用 id
穿搭用 outfit
我的搭配用 recordId
```

如果现有工程路由已有固定参数：

```text
主代理统一适配
```

三个子代理不得各自新增不同参数协议。

---

# 16. 穿搭详情统一入口

所有已保存搭配均先进入同一个：

```text
穿搭详情
```

详情统一支持：

```text
查看
编辑
设为今日穿搭
删除
```

其中：

```text
编辑
→ 才加载到穿搭主页面
```

禁止列表卡直接进入编辑模式。

---

# 17. 编辑已有搭配协议

穿搭详情点击：

```text
编辑
```

后需传递：

```text
outfitId
mode=edit
```

或现有工程的等价上下文。

编辑完成后保存必须提供：

```text
更新原搭配
另存为新搭配
取消
```

### 更新原搭配

```text
原 outfitId 不变
```

### 另存为新搭配

```text
生成新 outfitId
原记录不变
```

---

# 18. 首页智能匹配协议

首页筛选：

```ts
season: Season
style: OutfitStyle | 'all'
```

流程：

```text
首页选择季节
→ 选择风格
→ 查看匹配穿搭
→ 匹配结果页
```

匹配范围：

```text
仅已保存搭配
```

禁止：

```text
临时从衣橱自动拼一套
```

如果：

```text
style = all
```

则：

```text
只按 season 筛选
```

---

# 18.1 首页智能筛选默认状态

首页首次进入或没有已保存筛选状态时，统一默认：

```text
季节：当前季节
风格：不限
```

当前季节必须调用公共 `getCurrentSeason()`，不得自行复制月份判断。

用户本次在首页修改筛选条件时，只改变首页筛选状态，不改变穿搭保存数据。

---

# 19. 「换一套」公共规则

穿搭主页面：

```text
换一套
```

规则固定：

```text
当前季节
+
风格不限
+
从当前有效衣橱单品中随机推荐
```

只使用：

```text
deletedAt == null
```

的单品。

至少需要：

```text
上衣
下装
鞋子
```

帽子、包包可空。

不得：

- 使用历史已删除单品。
- 生成不存在的单品。
- 自动保存。
- 自动设为今日穿搭。

---

# 20. 当前季节判断

三个子代理不得分别实现三套季节判断。

统一公共函数：

```ts
getCurrentSeason()
```

V1 固定按**用户设备本地日期的月份**判断：

```text
3～5 月   → spring / 春
6～8 月   → summer / 夏
9～11 月  → autumn / 秋
12～2 月  → winter / 冬
```

首页智能筛选的默认季节、穿搭「换一套」均调用同一函数。

该规则不跟随用户手动选择的天气城市变化；城市只影响天气数据。V1 暂不根据城市气候带动态改变季节定义。

---

# 21. 城市 / 天气公共状态

首页负责天气 UI，但城市信息属于可共享状态。

最低语义：

```ts
type LocationState = {
  cityName: string
  latitude?: number
  longitude?: number
  source: 'device' | 'manual'
}
```

用户修改城市后：

```text
当前城市
当前温度
天气状态
天气图标
温度区间
天气建议
```

必须全部同步。

衣橱 / 穿搭子代理不得自行实现另一套城市状态。

---

# 22. 首页最近保存

首页最近保存：

```text
最多 4 套
按保存时间倒序
```

读取统一 `SavedOutfit`。

衣橱子代理不负责生成首页推荐。

穿搭保存成功后必须确保首页能重新读取最新记录。

---

# 23. 删除衣橱单品后的跨模块规则

衣橱删除：

```text
WardrobeItem
```

之后：

## 衣橱

- 不展示。
- 不参与搜索 / 筛选。
- 数量减少。

## 穿搭

- 新组合不可选。
- 换一套不可用。
- 历史快照继续显示。
- 编辑历史搭配时提示重新选择。

## 首页

- 最近保存 / 今日穿搭如引用该单品：
  - 继续显示历史搭配快照。
  - 不得丢图。

---

# 24. 三代理代码修改边界

## 子代理 A：首页

负责：

- 首页视觉。
- 首页四模块。
- 城市切换入口。
- 天气联动。
- 最近保存。
- 智能筛选 UI。
- 首页相关跳转。
- 首页加载 / 空 / 错状态。

不得负责：

- 衣橱上传流程。
- 穿搭五槽编辑器。
- 擅自重构 SavedOutfit 公共模型。

---

## 子代理 B：衣橱

负责：

- 衣橱主页面。
- 两列网格 / 列表视图。
- 搜索。
- 四类单选筛选。
- 五类分类。
- 上传三步流程。
- AI 识别结果确认。
- 单品详情 / 编辑 / 删除。
- 历史快照兼容所需的衣橱侧数据处理。

不得负责：

- 首页智能推荐。
- 穿搭「换一套」算法。
- 今日穿搭 UI。

---

## 子代理 C：穿搭

负责：

- 穿搭主页面。
- 叠穿预览。
- 五槽选择。
- 换一套。
- 保存搭配。
- 我的搭配。
- 穿搭详情。
- 编辑已有搭配。
- 更新原搭配 / 另存为新搭配。
- 今日穿搭设置入口。
- 首页快速开始对应的历史搭配选择流程。

不得负责：

- 衣橱上传表单。
- 首页天气。
- 擅自改变衣橱删除规则。

---

# 25. 公共文件修改规则

并发开发期间，以下类型文件属于：

```text
共享高冲突文件
```

例如：

- 全局类型定义。
- 全局路由配置。
- 数据库 schema。
- 云函数公共常量。
- 公共 store。
- 公共 API client。
- 底部 TabBar 配置。
- 公共枚举。
- 公共图片 / 快照数据模型。

原则：

```text
共享文件由主代理统一修改
```

如果子代理确实需要修改：

```text
先汇报需要修改什么
→ 主代理统一落地
```

避免三个子代理同时改同一文件。

---

# 26. 推荐并发方式

最稳妥的开发顺序：

```text
阶段 0：主代理
→ 先检查现有工程
→ 锁定公共类型 / 路由 / 数据库兼容
→ 必要时先建立共享契约

阶段 1：三个子代理并发
A 首页
B 衣橱
C 穿搭

阶段 2：主代理合并
→ 解决共享文件冲突

阶段 3：跨栏目联调

阶段 4：统一视觉验收
```

---

# 27. 子代理开始前必须先做

每个子代理在修改代码前必须：

```text
1. 阅读自己的栏目开发文档
2. 阅读本公共接口说明
3. 检查现有代码
4. 列出预计修改文件
5. 确认没有越过栏目边界
6. 再开始修改
```

不得：

```text
看完图片就直接重构整个项目
```

---

# 28. 三代理禁止事项

三个代理均禁止：

- 回滚覆盖当前工程。
- 删除现有数据兼容逻辑。
- 修改现有云函数 `TYPES`，除非主代理明确授权。
- 创建第四个底部 Tab。
- 恢复「配饰」正式分类。
- 添加真人试穿。
- 使用生成式服装图片。
- 擅自改变用户已经确认的按钮和操作流。
- 因为技术方便而改变设计图主要布局。
- 使用临时假数据作为正式结果。
- 为某件单品写特殊定位补丁。

---

# 29. 跨栏目联调清单

三代理完成后，由主代理统一验证：

## 衣橱 → 穿搭

- [ ] 新增上衣后穿搭上衣栏可见。
- [ ] 新增鞋子后穿搭鞋子栏可见。
- [ ] 修改分类后进入正确槽位。
- [ ] 删除后新搭配不可选。
- [ ] 删除后历史搭配仍显示快照。

## 穿搭 → 首页

- [ ] 新保存搭配后进入最近保存。
- [ ] 最近保存按时间倒序。
- [ ] 季节 / 风格可供首页筛选。
- [ ] 设置今日穿搭后首页更新。

## 首页 → 穿搭

- [ ] 快速开始先显示历史组合。
- [ ] 不自动选中。
- [ ] 去搭配单品进入穿搭主页面。
- [ ] 最近保存卡进入统一详情。
- [ ] 匹配结果进入统一详情。
- [ ] 详情点击编辑后加载正确五槽。

## 城市 / 天气

- [ ] 重新定位成功。
- [ ] 手动选择城市成功。
- [ ] 城市变化后天气全部同步。

---

# 30. 最终合并门禁

三个子代理分别完成，不代表 V1 开发完成。

主代理必须确认：

```text
3 个栏目单独验收通过
+
公共数据结构一致
+
跨栏目跳转一致
+
无共享文件冲突
+
历史数据兼容
+
删除快照逻辑正确
+
视觉仍符合参考图
```

才可以宣布三栏目开发阶段完成。

---

# 31. Codex 并发启动建议

主代理可按以下方式分派：

```text
子代理 A
只开发首页。
严格执行《小衣橱-首页开发文档-V1.md》和公共接口说明。
不得修改衣橱、穿搭业务逻辑。

子代理 B
只开发衣橱。
严格执行《小衣橱-衣橱开发文档-V1.md》和公共接口说明。
不得修改首页、穿搭业务逻辑。

子代理 C
只开发穿搭。
严格执行《小衣橱-穿搭开发文档-V1.md》和公共接口说明。
不得修改首页、衣橱业务逻辑。
```

所有共享类型、路由、数据库 schema、全局 store 的修改：

```text
统一交给 Codex 主代理处理
```

---

# 32. 并发开发完成定义

满足以下全部条件后，才适合正式同时启动三个子代理：

```text
三个栏目开发文档齐全
+
公共接口协议齐全
+
公共枚举统一
+
Outfit / WardrobeItem 语义统一
+
outfitId 跳转协议统一
+
删除快照规则统一
+
今日穿搭规则统一
+
三个代理文件修改边界明确
+
共享文件归主代理控制
```

本文件完成后，即满足并发开发的文档准备条件。
